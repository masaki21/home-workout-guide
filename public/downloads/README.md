# Legacy Downloads

The current product is an online HTML access guide.

No paid product files should be served from this public folder in v1.

Legacy exports were moved outside `public/` so they cannot be downloaded directly by URL.
